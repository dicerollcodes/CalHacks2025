{
  "name": "shatter-the-ice",
  "version": "1.0.0",
  "description": "Full-stack app to help students connect through shared interests",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "seed": "npm run seed --prefix server",
    "dev": "echo 'Run npm run dev:server in one terminal and npm run dev:client in another'"
  },
  "keywords": ["social", "matching", "interests", "college"],
  "author": "",
  "license": "MIT"
}
